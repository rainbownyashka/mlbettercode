# Creative+ Coding Guide

## Variable types

| Item | Type | Notes |
| --- | --- | --- |
| 📕 `minecraft:book` | Текст | Вводишь текст в чат при помощи удержания предмета. Поддерживаются цветовые коды. |
| 🟢 `minecraft:slime_ball` | Число | Ввод чисел/времени в тиках/π/e через чат. |
| 📜 `minecraft:paper` | Местоположение | ЛКМ телепортирует, Shift+ЛКМ перемещает к сохранённой точке, РК сохраняет координаты. |
| 🧪 `minecraft:glass_bottle` | Эффекты зелья | Правый клик зельем/чатом устанавливает эффект, длительность и усиление. |
| 🔥 `minecraft:magma_cream` | Динамическая переменная | Устанавливается через `Установить переменную`; поддерживает имена вида `%player%`. Shift+РК сохраняет. |
| 🍏 `minecraft:apple` | Игровое значение | Нет прямого изменения — зависит от состояния игры. |
| ⭐ `minecraft:nether_star` | Эффект частицы | Правый клик выбирает тип частицы. |
| 🖼 `minecraft:item_frame` | Массив | Shift+ПКМ делает «постоянным»; можно использовать для групп. |
| 🧠 `minecraft:shulker_shell` | Текстовый компонент | Хранит стилизованный текст, включая цвета. |
| 🐚 `minecraft:prismarine_shard` | Вектор | Хранит направление, устанавливается через чат. |
| 💎 `minecraft:emerald_block` | Блок цикла | Настройка задержки правым/Shift+ПКМ; запускается блоком «Запустить цикл». |
| 🟦 `minecraft:lapis_block` | Функция | Задаёшь имя через табличку; функции можно вызывать другим блоком. |
| ⛏ `minecraft:lapis_ore` | Вызвать функцию | Запускает объявления функций. |
| 🧸 `minecraft:slime_ball (второй)` | Особый режим `3` | Спец‑значение, используется в UI. |
| 🧱 `minecraft:beacon` | Копирователь кода | Shift+ЛКМ по алмазному блоку копирует строку. |
| 🧱 `minecraft:red_nether_brick` и остальные | Условия/Действия | См. таблички: `Если игра`, `Иначе`, `Если существо`, `Массивы`, `Событие игрока`, `Если игрок`, `Действие игрока`, `Игровое действие`, `Если значение`, `Установить переменную`, `Утилиты разработчика`, `Переменные`. |

## Работа с GUI

- Открытие «Утилиты разработчика» (`minecraft:gold_ingot` ПКМ) и «Переменные» (`minecraft:iron_ingot` ПКМ) доставляет набор предметов для быстрого создания кода.  
- Shift+ПКМ по предмету открывает редактор, который запоминает последний текст/число.  
- Нажатие `G` + колесо/клавиши `+`/`-` телепортирует по очереди шагов (10 → 5 → 3 → 2 → 1).  
- `/place` использует последние стекла и клавиши меню (категории) для автоматической расстановки.

## Быстрые действия

- `/autocacheall` (DEV): очередь из предметов инвентаря ставится на синее стекло, затем на серое, чтобы собрать кеш.  
- `/exportline` читает табличку над стеклом: если нет таблички, ничего не выводится.  
- `/tp pathfind` последовательность шагов (10…1) создаёт очередь телепортаций по пути.

## Сохранённые состояния

- Все кэши (меню, клики, стекло, сундуки) сохраняются в конфиге: `mcpythonapi_menu_cache.dat`, `mcpythonapi_click_menu.dat`, `mcpythonapi_code_glass.dat`, `mcpythonapi_chest_cache.dat`.  
- При старте `/place` использует `lastGlassPos` и `codeBlueGlassById`, если задана табличка с `ID`.
