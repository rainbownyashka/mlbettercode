# Forge 1.16.5 bootstrap

This module is a Java/bootstrap artifact that wires shared `modern/core` code into a dedicated `forge1165` target.

It is intentionally bootstrap-only in this commit and does not yet include Forge runtime hooks.
